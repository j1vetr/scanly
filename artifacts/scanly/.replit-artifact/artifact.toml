kind = "mobile"
previewPath = "/"
title = "Scanly"
version = "1.0.0"
id = "artifacts/scanly"
router = "expo-domain"

[[integratedSkills]]
name = "expo"
version = "1.0.0"

[[services]]
ensurePreviewReachable = "/status"
name = "expo"
paths = [ "/" ]
localPort = 25567

[services.development]
run = "pnpm --filter @workspace/scanly run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/scanly", "run", "build" ]
run = [ "pnpm", "--filter", "@workspace/scanly", "run", "serve" ]

[services.env]
PORT = "25567"
BASE_PATH = "/"
